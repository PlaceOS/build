name: placeos-build
version: 0.1.0
crystal: ~> 0.36
license: MIT

targets:
  build:
    main: src/build_app.cr

dependencies:
  shards:
    github: crystal-lang/shards

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
